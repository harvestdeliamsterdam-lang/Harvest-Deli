# Harvest Deli — robots.txt
# Indexation is controlled per-page with <meta name="robots"> (noindex on
# account/checkout/cart/admin/order/search pages). CSS, JS and image assets
# are intentionally crawlable so Google can render and evaluate the pages.

User-agent: *
Allow: /

# Private / transactional endpoints (also carry meta noindex)
Disallow: /checkout.html
Disallow: /admin.html
Disallow: /invoice.html
Disallow: /order-success.html

Sitemap: https://harvestdeli.nl/sitemap.xml
